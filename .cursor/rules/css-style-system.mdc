---
description: 用于CSS样式开发、样式架构、主题切换、响应式设计等样式相关问题
alwaysApply: false
---

# CSS样式系统规范

## 核心架构原则

### 模块化CSS架构

- **文件组织**: 按功能模块拆分CSS文件，保持单一职责
- **导入顺序**: 严格按照 tokens → base → layout → components → utilities → themes → responsive 的顺序
- **命名规范**: 使用语义化命名，避免样式化命名
- **依赖管理**: 通过统一入口文件管理所有样式导入

### 设计令牌系统

- **CSS变量优先**: 所有设计相关的值必须使用CSS变量，禁止硬编码
- **分层架构**: 基础令牌 → 语义令牌 → 组件令牌的层级结构
- **命名约定**: 使用 `--{category}-{property}-{variant}` 格式
- **主题支持**: 所有令牌必须支持浅色/深色/高对比度主题

### 工具类设计原则

- **原子化**: 每个工具类只负责一个样式属性
- **一致性**: 使用统一的命名模式和数值系统
- **响应式**: 重要工具类需要提供响应式变体
- **性能考虑**: 避免过度膨胀，只提供必要的工具类

## 文件结构标准

```
src/assets/styles/
├── index.css          # 唯一入口文件
├── tokens.css         # 设计令牌定义
├── base.css          # 重置和基础样式
├── layout.css        # 布局系统
├── components.css    # 组件样式库
├── utilities.css     # 工具类系统
├── themes.css        # 主题系统
└── responsive.css    # 响应式规则
```

## CSS变量命名规范

### 色彩系统

```css
/* 主色调 */
--color-primary
--color-secondary
--color-background
--color-surface
--color-surface-elevated

/* 文本色彩 */
--color-text-primary
--color-text-secondary
--color-text-muted
--color-text-inverse

/* 功能色彩 */
--color-success
--color-warning
--color-error
--color-info

/* 边框色彩 */
--color-border-light
--color-border-default
--color-border-strong
--color-border-focus
```

### 间距系统

```css
/* 使用t-shirt尺寸和数字系统 */
--space-0: 0 --space-1: 0.25rem /* 4px */ --space-2: 0.5rem /* 8px */ --space-3: 0.75rem /* 12px */
  --space-4: 1rem /* 16px */ --space-5: 1.25rem /* 20px */ --space-6: 1.5rem /* 24px */
  --space-8: 2rem /* 32px */ --space-10: 2.5rem /* 40px */ --space-12: 3rem /* 48px */;
```

### 字体系统

```css
/* 字体族 */
--font-family-sans
--font-family-mono

/* 字号 */
--font-size-xs: 0.75rem
--font-size-sm: 0.875rem
--font-size-base: 1rem
--font-size-lg: 1.125rem
--font-size-xl: 1.25rem
--font-size-2xl: 1.5rem

/* 字重 */
--font-weight-light: 300
--font-weight-normal: 400
--font-weight-medium: 500
--font-weight-semibold: 600
--font-weight-bold: 700

/* 行高 */
--line-height-tight: 1.25
--line-height-normal: 1.5
--line-height-relaxed: 1.75
```

## 组件样式规范

### 基础组件结构

```css
.component-name {
  /* 1. 布局相关属性 */
  display: flex;
  position: relative;

  /* 2. 尺寸相关属性 */
  width: 100%;
  height: var(--component-height);

  /* 3. 间距相关属性 */
  padding: var(--space-4);
  margin: 0;

  /* 4. 字体相关属性 */
  font-family: var(--font-family-sans);
  font-size: var(--font-size-base);

  /* 5. 颜色相关属性 */
  color: var(--color-text-primary);
  background-color: var(--color-surface);
  border: 1px solid var(--color-border-default);

  /* 6. 视觉效果属性 */
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);

  /* 7. 过渡和动画 */
  transition: all var(--duration-fast) var(--easing-smooth);
}
```

### 组件变体规范

```css
/* 尺寸变体 */
.btn-sm {
  height: var(--button-height-sm);
}
.btn-base {
  height: var(--button-height-base);
}
.btn-lg {
  height: var(--button-height-lg);
}

/* 样式变体 */
.btn-primary {
  /* 主要样式 */
}
.btn-secondary {
  /* 次要样式 */
}
.btn-ghost {
  /* 幽灵样式 */
}

/* 状态变体 */
.btn:hover {
  /* 悬停状态 */
}
.btn:focus {
  /* 焦点状态 */
}
.btn:disabled {
  /* 禁用状态 */
}
```

### 状态管理规范

```css
/* 交互状态必须包含 */
.component:hover {
  /* 悬停效果 */
}

.component:focus {
  /* 焦点样式 */
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

.component:active {
  /* 激活状态 */
}

.component:disabled {
  /* 禁用状态 */
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}
```

## 工具类规范

### 命名约定

- **间距**: `.p-{size}`, `.m-{size}`, `.px-{size}`, `.py-{size}`
- **字体**: `.text-{size}`, `.font-{weight}`, `.leading-{height}`
- **颜色**: `.text-{color}`, `.bg-{color}`, `.border-{color}`
- **布局**: `.flex`, `.grid`, `.block`, `.hidden`
- **定位**: `.relative`, `.absolute`, `.fixed`

### 响应式工具类

```css
/* 基础工具类 */
.hidden {
  display: none;
}

/* 响应式变体 */
.md:hidden {
  /* 平板及以上隐藏 */
}
.lg:hidden {
  /* 桌面及以上隐藏 */
}

/* 显示控制 */
.mobile-only {
  /* 仅手机显示 */
}
.tablet-only {
  /* 仅平板显示 */
}
.desktop-only {
  /* 仅桌面显示 */
}
```

## 响应式设计标准

### 断点系统

```css
/* Mobile First 策略 */
/* 基础样式: < 640px (手机) */
@media (min-width: 640px) {
  /* sm: 大手机 */
}
@media (min-width: 768px) {
  /* md: 平板 */
}
@media (min-width: 1024px) {
  /* lg: 小桌面 */
}
@media (min-width: 1280px) {
  /* xl: 大桌面 */
}
@media (min-width: 1536px) {
  /* 2xl: 超大桌面 */
}
```

### 响应式布局原则

- **Mobile First**: 从最小屏幕开始设计，向上扩展
- **渐进增强**: 在更大屏幕上增加功能，而不是减少
- **触摸友好**: 移动端按钮最小44px，间距适当
- **内容优先**: 确保核心内容在所有设备上可访问

## 主题系统规范

### 主题变量结构

```css
:root {
  /* 浅色主题（默认） */
  --color-background: #ffffff;
  color-scheme: light;
}

[data-theme='dark'] {
  /* 深色主题 */
  --color-background: #0f0f0f;
  color-scheme: dark;
}

[data-theme='high-contrast'] {
  /* 高对比度主题 */
  --color-background: #ffffff;
  color-scheme: light;
}
```

### 主题切换机制

```javascript
// 主题切换函数
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('theme', theme);
}

// 系统主题检测
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
```

## 可访问性要求

### 颜色对比度

- **正文文字**: 对比度 ≥ 4.5:1
- **大文字(18px+)**: 对比度 ≥ 3:1
- **非文字元素**: 对比度 ≥ 3:1
- **状态指示**: 不能仅依赖颜色

### 焦点管理

```css
/* 焦点指示器 */
.focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

/* 跳过链接 */
.skip-link {
  position: absolute;
  top: -40px;
  left: 6px;
  background: var(--color-background);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-md);
  transition: top var(--duration-fast) var(--easing-ease-out);
}

.skip-link:focus {
  top: 6px;
}
```

### 动画减少支持

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

## 性能优化规范

### CSS加载优化

- **模块化加载**: 按需加载样式模块
- **关键路径CSS**: 内联关键样式，异步加载非关键样式
- **文件压缩**: 使用CSS压缩工具
- **缓存策略**: 合理设置缓存头

### 渲染性能优化

```css
/* 使用GPU加速 */
.animate {
  transform: translateZ(0);
  will-change: transform;
}

/* 避免重排的动画 */
.smooth-transform {
  transition: transform var(--duration-normal) var(--easing-smooth);
}

/* 不要动画这些属性 */
/* width, height, padding, margin, border */

/* 优先动画这些属性 */
/* transform, opacity, filter */
```

## 代码质量标准

### CSS检查清单

- [ ] 使用CSS变量而非硬编码值
- [ ] 遵循模块化文件组织
- [ ] 实现响应式设计
- [ ] 支持主题切换
- [ ] 符合可访问性标准
- [ ] 性能优化到位
- [ ] 命名规范统一

### 禁止使用

```css
/* ❌ 禁止硬编码颜色 */
.bad {
  color: #ff0000;
}

/* ❌ 禁止魔法数字 */
.bad {
  margin: 13px;
}

/* ❌ 禁止important */
.bad {
  color: red !important;
}

/* ❌ 禁止ID选择器 */
#bad {
  color: red;
}

/* ✅ 正确方式 */
.good {
  color: var(--color-error);
  margin: var(--space-3);
}
```

### 必须遵循

```css
/* ✅ 必须使用设计令牌 */
.component {
  background-color: var(--color-surface);
  padding: var(--space-4);
  border-radius: var(--radius-md);
}

/* ✅ 必须提供焦点样式 */
.interactive:focus {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
}

/* ✅ 必须考虑主题切换 */
.component {
  color: var(--color-text-primary); /* 自动适配主题 */
}
```

## 维护和更新

### 版本管理

- **设计令牌版本**: 重大变更需要更新版本号
- **向下兼容**: 新版本必须兼容旧的API
- **废弃通知**: 提前一个版本标记废弃的属性
- **迁移指南**: 提供详细的升级指南

### 文档维护

- **组件文档**: 每个组件都有完整的使用文档
- **示例代码**: 提供常见用法的示例
- **最佳实践**: 持续更新最佳实践指南
- **变更日志**: 记录所有变更和原因

**版本**: v1.0.0
**创建时间戳**: T275.3
**最后更新**: T275.3
**状态**: 🟢 CURRENT
**维护者**: InkDot开发团队
**下次审查**: T305.3

# CSS样式系统规范

## 核心架构原则

### 模块化CSS架构

- **文件组织**: 按功能模块拆分CSS文件，保持单一职责
- **导入顺序**: 严格按照 tokens → base → layout → components → utilities → themes → responsive 的顺序
- **命名规范**: 使用语义化命名，避免样式化命名
- **依赖管理**: 通过统一入口文件管理所有样式导入

### 设计令牌系统

- **CSS变量优先**: 所有设计相关的值必须使用CSS变量，禁止硬编码
- **分层架构**: 基础令牌 → 语义令牌 → 组件令牌的层级结构
- **命名约定**: 使用 `--{category}-{property}-{variant}` 格式
- **主题支持**: 所有令牌必须支持浅色/深色/高对比度主题

### 工具类设计原则

- **原子化**: 每个工具类只负责一个样式属性
- **一致性**: 使用统一的命名模式和数值系统
- **响应式**: 重要工具类需要提供响应式变体
- **性能考虑**: 避免过度膨胀，只提供必要的工具类

## 文件结构标准

```
src/assets/styles/
├── index.css          # 唯一入口文件
├── tokens.css         # 设计令牌定义
├── base.css          # 重置和基础样式
├── layout.css        # 布局系统
├── components.css    # 组件样式库
├── utilities.css     # 工具类系统
├── themes.css        # 主题系统
└── responsive.css    # 响应式规则
```

## CSS变量命名规范

### 色彩系统

```css
/* 主色调 */
--color-primary
--color-secondary
--color-background
--color-surface
--color-surface-elevated

/* 文本色彩 */
--color-text-primary
--color-text-secondary
--color-text-muted
--color-text-inverse

/* 功能色彩 */
--color-success
--color-warning
--color-error
--color-info

/* 边框色彩 */
--color-border-light
--color-border-default
--color-border-strong
--color-border-focus
```

### 间距系统

```css
/* 使用t-shirt尺寸和数字系统 */
--space-0: 0 --space-1: 0.25rem /* 4px */ --space-2: 0.5rem /* 8px */ --space-3: 0.75rem /* 12px */
  --space-4: 1rem /* 16px */ --space-5: 1.25rem /* 20px */ --space-6: 1.5rem /* 24px */
  --space-8: 2rem /* 32px */ --space-10: 2.5rem /* 40px */ --space-12: 3rem /* 48px */;
```

### 字体系统

```css
/* 字体族 */
--font-family-sans
--font-family-mono

/* 字号 */
--font-size-xs: 0.75rem
--font-size-sm: 0.875rem
--font-size-base: 1rem
--font-size-lg: 1.125rem
--font-size-xl: 1.25rem
--font-size-2xl: 1.5rem

/* 字重 */
--font-weight-light: 300
--font-weight-normal: 400
--font-weight-medium: 500
--font-weight-semibold: 600
--font-weight-bold: 700

/* 行高 */
--line-height-tight: 1.25
--line-height-normal: 1.5
--line-height-relaxed: 1.75
```

## 组件样式规范

### 基础组件结构

```css
.component-name {
  /* 1. 布局相关属性 */
  display: flex;
  position: relative;

  /* 2. 尺寸相关属性 */
  width: 100%;
  height: var(--component-height);

  /* 3. 间距相关属性 */
  padding: var(--space-4);
  margin: 0;

  /* 4. 字体相关属性 */
  font-family: var(--font-family-sans);
  font-size: var(--font-size-base);

  /* 5. 颜色相关属性 */
  color: var(--color-text-primary);
  background-color: var(--color-surface);
  border: 1px solid var(--color-border-default);

  /* 6. 视觉效果属性 */
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);

  /* 7. 过渡和动画 */
  transition: all var(--duration-fast) var(--easing-smooth);
}
```

### 组件变体规范

```css
/* 尺寸变体 */
.btn-sm {
  height: var(--button-height-sm);
}
.btn-base {
  height: var(--button-height-base);
}
.btn-lg {
  height: var(--button-height-lg);
}

/* 样式变体 */
.btn-primary {
  /* 主要样式 */
}
.btn-secondary {
  /* 次要样式 */
}
.btn-ghost {
  /* 幽灵样式 */
}

/* 状态变体 */
.btn:hover {
  /* 悬停状态 */
}
.btn:focus {
  /* 焦点状态 */
}
.btn:disabled {
  /* 禁用状态 */
}
```

### 状态管理规范

```css
/* 交互状态必须包含 */
.component:hover {
  /* 悬停效果 */
}

.component:focus {
  /* 焦点样式 */
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

.component:active {
  /* 激活状态 */
}

.component:disabled {
  /* 禁用状态 */
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}
```

## 工具类规范

### 命名约定

- **间距**: `.p-{size}`, `.m-{size}`, `.px-{size}`, `.py-{size}`
- **字体**: `.text-{size}`, `.font-{weight}`, `.leading-{height}`
- **颜色**: `.text-{color}`, `.bg-{color}`, `.border-{color}`
- **布局**: `.flex`, `.grid`, `.block`, `.hidden`
- **定位**: `.relative`, `.absolute`, `.fixed`

### 响应式工具类

```css
/* 基础工具类 */
.hidden {
  display: none;
}

/* 响应式变体 */
.md:hidden {
  /* 平板及以上隐藏 */
}
.lg:hidden {
  /* 桌面及以上隐藏 */
}

/* 显示控制 */
.mobile-only {
  /* 仅手机显示 */
}
.tablet-only {
  /* 仅平板显示 */
}
.desktop-only {
  /* 仅桌面显示 */
}
```

## 响应式设计标准

### 断点系统

```css
/* Mobile First 策略 */
/* 基础样式: < 640px (手机) */
@media (min-width: 640px) {
  /* sm: 大手机 */
}
@media (min-width: 768px) {
  /* md: 平板 */
}
@media (min-width: 1024px) {
  /* lg: 小桌面 */
}
@media (min-width: 1280px) {
  /* xl: 大桌面 */
}
@media (min-width: 1536px) {
  /* 2xl: 超大桌面 */
}
```

### 响应式布局原则

- **Mobile First**: 从最小屏幕开始设计，向上扩展
- **渐进增强**: 在更大屏幕上增加功能，而不是减少
- **触摸友好**: 移动端按钮最小44px，间距适当
- **内容优先**: 确保核心内容在所有设备上可访问

## 主题系统规范

### 主题变量结构

```css
:root {
  /* 浅色主题（默认） */
  --color-background: #ffffff;
  color-scheme: light;
}

[data-theme='dark'] {
  /* 深色主题 */
  --color-background: #0f0f0f;
  color-scheme: dark;
}

[data-theme='high-contrast'] {
  /* 高对比度主题 */
  --color-background: #ffffff;
  color-scheme: light;
}
```

### 主题切换机制

```javascript
// 主题切换函数
function setTheme(theme) {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('theme', theme);
}

// 系统主题检测
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
```

## 可访问性要求

### 颜色对比度

- **正文文字**: 对比度 ≥ 4.5:1
- **大文字(18px+)**: 对比度 ≥ 3:1
- **非文字元素**: 对比度 ≥ 3:1
- **状态指示**: 不能仅依赖颜色

### 焦点管理

```css
/* 焦点指示器 */
.focus-visible {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
  outline-offset: var(--focus-ring-offset);
}

/* 跳过链接 */
.skip-link {
  position: absolute;
  top: -40px;
  left: 6px;
  background: var(--color-background);
  padding: var(--space-2) var(--space-4);
  border-radius: var(--radius-md);
  transition: top var(--duration-fast) var(--easing-ease-out);
}

.skip-link:focus {
  top: 6px;
}
```

### 动画减少支持

```css
@media (prefers-reduced-motion: reduce) {
  *,
  *::before,
  *::after {
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
```

## 性能优化规范

### CSS加载优化

- **模块化加载**: 按需加载样式模块
- **关键路径CSS**: 内联关键样式，异步加载非关键样式
- **文件压缩**: 使用CSS压缩工具
- **缓存策略**: 合理设置缓存头

### 渲染性能优化

```css
/* 使用GPU加速 */
.animate {
  transform: translateZ(0);
  will-change: transform;
}

/* 避免重排的动画 */
.smooth-transform {
  transition: transform var(--duration-normal) var(--easing-smooth);
}

/* 不要动画这些属性 */
/* width, height, padding, margin, border */

/* 优先动画这些属性 */
/* transform, opacity, filter */
```

## 代码质量标准

### CSS检查清单

- [ ] 使用CSS变量而非硬编码值
- [ ] 遵循模块化文件组织
- [ ] 实现响应式设计
- [ ] 支持主题切换
- [ ] 符合可访问性标准
- [ ] 性能优化到位
- [ ] 命名规范统一

### 禁止使用

```css
/* ❌ 禁止硬编码颜色 */
.bad {
  color: #ff0000;
}

/* ❌ 禁止魔法数字 */
.bad {
  margin: 13px;
}

/* ❌ 禁止important */
.bad {
  color: red !important;
}

/* ❌ 禁止ID选择器 */
#bad {
  color: red;
}

/* ✅ 正确方式 */
.good {
  color: var(--color-error);
  margin: var(--space-3);
}
```

### 必须遵循

```css
/* ✅ 必须使用设计令牌 */
.component {
  background-color: var(--color-surface);
  padding: var(--space-4);
  border-radius: var(--radius-md);
}

/* ✅ 必须提供焦点样式 */
.interactive:focus {
  outline: var(--focus-ring-width) solid var(--focus-ring-color);
}

/* ✅ 必须考虑主题切换 */
.component {
  color: var(--color-text-primary); /* 自动适配主题 */
}
```

## 维护和更新

### 版本管理

- **设计令牌版本**: 重大变更需要更新版本号
- **向下兼容**: 新版本必须兼容旧的API
- **废弃通知**: 提前一个版本标记废弃的属性
- **迁移指南**: 提供详细的升级指南

### 文档维护

- **组件文档**: 每个组件都有完整的使用文档
- **示例代码**: 提供常见用法的示例
- **最佳实践**: 持续更新最佳实践指南
- **变更日志**: 记录所有变更和原因

**版本**: v1.0.0
**创建时间戳**: T275.3
**最后更新**: T275.3
**状态**: 🟢 CURRENT
**维护者**: InkDot开发团队
**下次审查**: T305.3
